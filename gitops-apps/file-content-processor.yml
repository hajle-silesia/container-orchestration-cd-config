apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: file-content-processor
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/hajle-silesia/file-content-processor-config.git
    targetRevision: HEAD
    path: helm
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      selfHeal: true
      prune: true
