apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: file-content-frontend
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/hajle-silesia/frontend-config.git
    targetRevision: HEAD
    path: helm
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      selfHeal: true
      prune: true
